<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1>
<%= current_user.name %>
<% if @sticked_micropost.user.favorite_team.present? %>
    <span class="badge-team <%= @sticked_micropost.user.favorite_team.parameterize %>">
      <%= @sticked_micropost.user.favorite_team %>
    </span>
  <% end %>
</h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
